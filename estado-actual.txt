## Estado actual del proyecto — Marketing DEPA IA

Actualizado: 2026-08-30
Último foco: producción VPS (InsightFlow), integración X en prod, OpenRouter LLM, documentación OAuth/redes.

**Producción:** `https://marketing.powerupsecosistem.online` · VPS `insightflow@2.25.107.229` · compose `marketing-depa-prod`  
**InsightFlow (no tocar):** `https://api.powerupsecosistem.online`

**NotebookLM:** [`docs/notebooklm/Marketing-DEPA-IA-fuente-completa.md`](docs/notebooklm/Marketing-DEPA-IA-fuente-completa.md) · [`docs/notebooklm/COMO-SUBIR.md`](docs/notebooklm/COMO-SUBIR.md)

---

### Cronología reciente

#### 2026-08-30 — Producción, X y documentación operativa

| Entrega | Detalle |
|---------|---------|
| **VPS live** | Stack en `~/apps/marketing-depa-ia`; Caddy del host; loopback `8000`/`8081`; coexistencia con InsightFlow |
| **Dominio prod** | `marketing.powerupsecosistem.online` — Meta/X callbacks y `PUBLIC_IMAGE_BASE_URL` migrados desde ngrok |
| **X OAuth 1.0a + publish** | `auth_social.py` (login/callback), `social_providers.py` (media v1.1 + POST `/2/tweets`), UI **Conectar X** |
| **X tokens en `.env`** | `X_ACCESS_TOKEN` / `X_ACCESS_TOKEN_SECRET` como fallback si no hay cuenta en Integraciones; `X_CLIENT_ID` / `X_CLIENT_SECRET` (OAuth 2.0, reservados) |
| **VPS credenciales X** | `.env.production` sincronizado; `api` + `worker` + `video-worker` reconstruidos |
| **OpenRouter LLM** | `OPENAI_API_BASE` + `OPENAI_MODEL` en `llm.py`/`settings.py`; recomendado en VPS (`LLM_PROVIDER=openai`) |
| **Legales + TikTok verify** | `/terminos`, `/privacidad`, `tiktok*.txt` vía FastAPI; app TikTok **en App Review** |
| **Docs deploy** | `proceso-integracion-redes.md`, `x-oauth-production.md`, `tiktok-app-review.md`, `meta-oauth-production.md`, `vps-hostinger.md` |
| **Go publisher prod** | Fixes Dockerfile: `internal/` + build estático `CGO_ENABLED=0` |

#### 2026-08-11 — Formatos por red social + universal

| Entrega | Detalle |
|---------|---------|
| **Catálogo por red** | `image_specs.py` gana `_NETWORK_FORMATS`, `NETWORKS`, `CONTENT_FORMATS`, `VIDEO_FORMATS` y `formats_for_network()`: cada red expone solo sus formatos válidos (LinkedIn = feed + universal; TikTok sin feed horizontal; X sin historias) |
| **`content_format=universal`** | Pieza **1080×1080** idéntica en todas las redes, para publicar el mismo post en varias a la vez. En layout y publicación se comporta como `feed`, así que no toca video ni Shotstack |
| **Specs nuevas** | X 1200×675 (feed) y 1080×1920 (video); TikTok 1080×1920 |
| **Endpoint** | `GET /api/image/formats` → redes + formatos con `width`/`height`/`is_video`; el dashboard ya no hardcodea dimensiones (fallback local si la API cae) |
| **Dashboard** | Selects **Red social** + **Formato de publicación** juntos; al cambiar de red se sanea el formato seleccionado (elegir LinkedIn con `story` ya no manda un formato inválido). `red_social` sale del loop del brief |
| **Corte de publicación** | `_publish_run` devuelve `unavailable` para plataformas sin provider (TikTok/X): sin ese corte el sidecar Go recibía una plataforma que no soporta y `_mock` reportaba un falso éxito |
| **Tests** | 9 nuevos en `test_format_normalization.py` → **271 collected** |

#### 2026-08-09..10 — Hilo de pensamiento, contraste y video Venice

| Entrega | Detalle |
|---------|---------|
| **Hilo de pensamiento** | `thought_stream.py`: cada agente publica eventos en vivo por `trace_id` (Redis con degradado a memoria). `GET /api/thoughts/{trace_id}` + `POST /api/thoughts/{trace_id}/reply`; **modo interactivo** detiene el pipeline en checkpoints (`continue`/`adjust`/`cancel`). UI: `AgentThoughtThread.jsx` |
| **Contraste adaptativo** | `text_contrast.py` (`region_luminance`, `pick_text_colors`, `text_safe_box`) muestrea la luminancia del fondo → cierra la deuda "contraste no adaptativo" |
| **Guard anti-texto** | `visual_prompt_guards.py`: sufijo y negativos compartidos por fal/Venice/SD para que el modelo genere solo foto (la tipografía la pone Pillow) |
| **Modos de video** | `video_gen_mode` = `full` (clip AI completo) / `scenes` (Venice por escena + Shotstack) / `still` (Ken Burns); catálogo de modelos en `venice_video_models.py` (Seedance 2.5/2.0, Kling O3, MiniMax H3) vía `GET /api/video/options` |

#### 2026-08-09 — LinkedIn nativo: API versionada, expiry y guard de video

| Entrega | Detalle |
|---------|---------|
| **API versionada** | `social_providers._linkedin` migrado de `/v2/assets?action=registerUpload` + `/v2/ugcPosts` (deprecados) a `/rest/images?action=initializeUpload` + `/rest/posts` con header `LinkedIn-Version` (`LINKEDIN_API_VERSION`, default `202401`) |
| **Un solo camino** | Eliminado `microservices/social-publisher-go/internal/publisher/linkedin.go` (código muerto: el routing manda LinkedIn a Python). El sidecar Go ahora responde 400 explícito para `platform=linkedin` |
| **Expiry visible** | `_exchange_linkedin` persiste `expires_at` desde `expires_in` (~60 d); `/auth/accounts` y `/auth/status` devuelven `expires_at`, `expires_in_days`, `is_expired`, `expires_soon`; columna **Token** en Integraciones con aviso a ≤7 días |
| **Falla temprana** | `_assert_token_not_expired` corta antes de llamar a la API (LinkedIn y Meta) con mensaje accionable |
| **Guard de video** | LinkedIn nativo solo soporta imagen: `reel`/`user_clip_reel` daban `AttributeError` sobre `image_url=None`; ahora error explícito |
| **OAuth redirect** | URL de autorización armada con `urlencode` (LinkedIn exige `redirect_uri` codificado y match exacto contra *Authorized redirect URLs*) |
| **URN fallback** | `_linkedin_fetch_urn` usa `/v2/userinfo` (scope `profile`); `/v2/me` exigía `r_liteprofile`, que ya no se pide |

#### 2026-08-04 — Paquete NotebookLM + límites de revise/contraste

| Entrega | Detalle |
|---------|---------|
| **Fuente NotebookLM** | `docs/notebooklm/Marketing-DEPA-IA-fuente-completa.md` — documento autocontenido para indexar en NotebookLM |
| **Guía de subida** | `docs/notebooklm/COMO-SUBIR.md` |
| **Límites documentados** | Revise HITL: notas van al prompt de imagen/video, no al copywriter ni a Pillow; contraste de texto no es adaptativo al fondo |

#### 2026-08-03 — Manual de marca, Venice y tipografía premium

| Entrega | Detalle |
|---------|---------|
| **Manual PDF** | `POST/GET/DELETE /api/briefs/upload-brand-manual` — PDF ≤20 MB; texto pypdf + fallback **PaddleOCR** (`OCR_PROVIDER=paddle`) |
| **Escaneo visual** | `brand_scan.py`: logos embebidos (PyMuPDF) + recortes de cabecera + paleta dominante; `active.json` + `*_visual.json` |
| **Cues de diseño** | `brand_visual.py`: hex/RGB/CMYK/Pantone, fuentes, emociones; `resolve_brand_cues` → arquetipo `brand_campaign_piece`, prompts Flux/Venice y overlay Pillow con logo |
| **Tipografía premium** | Pack OFL en `static/fonts/` (Great Vibes / Playfair / Montserrat); roles display/body/cta/tagline |
| **Venice.ai** | Imagen síncrona + opcional `VIDEO_SCENE_PROVIDER=venice` (image-to-video por escena); `GET /api/image/providers` lista Venice si hay key |
| **Asesor creativo** | Burbuja UI + `POST /api/advisor/chat` (diseñador/productor/mercadólogo + contexto de marca) |
| **UX brief** | Label “Descripción del producto o evento”; preview de paleta/logos tras subir PDF; CTA en imagen opcional + `link_url` en caption |
| **Tests** | `test_brand_and_advisor`, `test_brand_scan`, `test_brand_visual`, `test_venice`, `test_premium_fonts`, `test_caption` → **214 collected** |

#### 2026-07-27 — Deuda técnica: Ollama, clips y CI/CD

Cold-start Ollama (`OLLAMA_KEEP_ALIVE`, `LLM_TIMEOUT_SECONDS`); `clip_public_url` con `PUBLIC_IMAGE_BASE_URL`; `skaffold.yaml` + `clouddeploy.yaml` + `video-worker-deployment.yaml`; `ffmpeg` en `Dockerfile.worker`.

#### 2026-07-26..27 — HITL revise + multi-cuenta

`POST /runs/{id}/revise` (migración `0006`); N cuentas OAuth por proveedor + selector **Cuenta destino** (migración `0007`).

#### 2026-07-03..25 — Núcleo MVP

Design-as-Code (foto usuario), LinkedIn nativo, Reels Shotstack, `user_clip_reel` (Drive→Whisper), doctrina inbound, fail-loudly fal/SD.

---

### Qué es el proyecto

MVP de automatización de marketing con agentes de IA:

```
brief (+ manual de marca) → estrategia → copy con QA → diseño (imagen / reel / clips)
  → aprobación humana (approve / reject / revise) → publicación Meta, LinkedIn o X
```

---

### Arquitectura operativa

**Local (dev):**

```
Frontend (React/Vite :5173)  + AdvisorChatBubble
        ↓
Gateway FastAPI (:8000)  ←→  PostgreSQL (:5433) + Redis (:6379)
        ↓
Celery (default) + Celery (`video_render`) + APScheduler
        ↓
Pipeline Python (agents/marketing_agents/)
        ↓
Go sidecar (:8088) → Meta Graph  |  Python nativo → LinkedIn, X
```

**Producción (VPS):** mismo stack en Docker (`infra/docker-compose.prod.yml`); Caddy del **host** termina TLS; API `:8000` y frontend `:8081` solo en loopback. Guía: `infra/deploy/vps-hostinger.md`.

| Capa | Tecnología | Estado |
|------|------------|--------|
| API | FastAPI + Alembic | ✅ |
| Agentes | LangGraph Copy/QA + pipeline lineal | ✅ |
| LLM | Ollama (local) / Anthropic / OpenAI / **OpenRouter** + stub | ✅ |
| Imágenes | **fal.ai**, **Venice.ai**, SD (A1111), DALL·E, mock | ✅ |
| Marca | PDF + PaddleOCR + scan paleta/logos + fonts OFL | ✅ |
| Asesor | Chat LLM con contexto de brief/marca | ✅ |
| Social Meta/IG | OAuth multi-cuenta + Go sidecar | ✅ prod en curso |
| Social LinkedIn | OAuth + publish nativo Python (imagen) | ✅ |
| Social **X** | OAuth 1.0a + publish nativo Python (tweet + imagen) | ✅ prod configurado; post prueba pendiente |
| Social TikTok | Generación + legales/verify; publish nativo | ⏳ App Review |
| Video | Shotstack + voz fal/ElevenLabs/OpenAI; Venice i2v | ✅ |
| Clips usuario | Drive + Whisper + ClipEditor + Shotstack | ✅ |
| HITL | pending_approval → approve/reject/revise | ✅ |
| Hilo de pensamiento | Eventos en vivo + checkpoints interactivos | ✅ |
| Formatos por red | Catálogo `image_specs` + universal 1:1 | ✅ |
| Deploy VPS | Hostinger + InsightFlow | ✅ live |
| CI/CD K8s | Skaffold/Cloud Deploy escritos, **sin clúster real** | 🟡 |

---

### Pipeline de agentes

```
Strategist (+ brand + inbound)
  → LangGraph [Copywriter ↔ QA]
  → Designer (feed/story) | VideoDesigner (reel) | ClipReelDesigner (user_clip_reel)
  → HITL → Publisher (cuenta = social_account_id)
```

**Módulos clave** (`agents/marketing_agents/`):

| Archivo | Rol |
|---------|-----|
| `strategist.py` / `copywriter.py` / `quality.py` / `graph_copy_qa.py` | Estrategia + copy con bucle QA |
| `brand_manual.py` / `ocr_paddle.py` / `brand_scan.py` / `brand_visual.py` | Manual PDF → texto + paleta + logos → cues |
| `designer.py` / `layout_archetypes.py` / `design_layouts.py` | Pieza visual + overlays |
| `image_specs.py` | Dimensiones y catálogo de formatos por red (fuente única) |
| `text_contrast.py` / `overlay_text.py` | Color de texto según luminancia del fondo + tipografía OFL |
| `visual_prompt_guards.py` | Sufijo/negativos anti-texto compartidos por los proveedores de imagen |
| `image_providers.py` / `venice_client.py` | fal / Venice / SD / DALL·E |
| `advisor.py` | Asesor creativo conversacional |
| `thought_stream.py` | Hilo de pensamiento: eventos en vivo + checkpoints interactivos |
| `video_*` / `venice_video_models.py` / `clip_*` / `drive_source.py` / `voice_providers.py` | Reels, modelos de video y clips |
| `knowledge/inbound_marketing.py` | Doctrina inbound |
| `caption.py` | Caption publicable (hashtags + link) |

---

### Motor visual y marca

**Proveedores de imagen:** `IMAGE_PROVIDER=fal|venice|stable_diffusion|openai|mock` (UI lista los que tengan key/URL).

**5 arquetipos** (`layout_archetypes.py`):

| ID | Cuándo |
|----|--------|
| `brand_campaign_piece` | Manual de marca activo (logo + foto full-bleed + tipografía centrada) — ver `docs/references/` |
| `typographic_poster` | Promocional / ventas |
| `minimal_conceptual` | Informativo |
| `editorial_infographic` | Educativo / branding (sin manual) |
| `cinematic_hero` | Storytelling / entretenimiento |

**Flujo brand book:**

1. Subir PDF → pypdf; si texto corto y `OCR_PROVIDER=paddle` → PaddleOCR  
2. `scan_brand_pdf` → paleta + logos PNG en `static/uploads/brand/{tenant}/`  
3. Brief carga `brand_context` + `brand_palette` + `brand_logo_paths`  
4. Designer fuerza campaña de marca, colores del scan, fonts OFL/Windows, pega logo  

**Stories:** overlay Pillow centrado. **CTA en imagen:** solo con `cta_on_image=true` (salvo pieza de campaña con marca). Hashtags/`link_url` van al caption. **Contraste:** `text_contrast.py` mide la luminancia de la zona del texto y elige color/viñeta.

---

### Formatos de publicación por red

Fuente única: `image_specs.py` (`_NETWORK_FORMATS` + `_SPECS`), expuesta por `GET /api/image/formats`.

| Red | Formatos | Feed |
|-----|----------|------|
| Instagram / Facebook | feed, story, reel, user_clip_reel, universal | 1080×1350 |
| LinkedIn | feed, universal | 1200×627 |
| TikTok | story, reel, user_clip_reel, universal | — (solo vertical 1080×1920) |
| X (Twitter) | feed, reel, universal | 1200×675 |

**`universal` = 1080×1080** idéntico en todas las redes: el encuadre que ninguna recorta agresivamente, para cuando la misma pieza va a varias redes. En layout y publicación se comporta como `feed`.

**X:** publicación nativa OAuth 1.0a (`social_providers.py`): tweet + imagen. Conectar vía Integraciones **o** `X_ACCESS_TOKEN` + `X_ACCESS_TOKEN_SECRET` en `.env`. Portal: permisos **Read and write**, tipo **Web App**, callback prod. **TikTok:** solo generación; app en App Review; OAuth/publish fase 2.

---

### Integración redes — producción

Estado vivo: **`infra/deploy/proceso-integracion-redes.md`**

| Red | Publish | Estado prod (2026-08-30) |
|-----|---------|-------------------------|
| Meta / IG / FB | Go sidecar | Portal migrado a dominio prod; Conectar Meta → prueba pendiente |
| LinkedIn | Python nativo | Redirect prod; conectar cuenta en dashboard |
| **X** | Python nativo | Portal + `.env.production` en VPS; post de prueba pendiente |
| TikTok | — | App Review; legales + verify `.txt` listos |

Variables X en prod: `X_API_KEY`, `X_API_SECRET`, `X_REDIRECT_URI`, opcional `X_ACCESS_TOKEN`, `X_ACCESS_TOKEN_SECRET`, `X_BEARER_TOKEN`, `X_CLIENT_ID`, `X_CLIENT_SECRET`. Tras cambios: `docker compose … up -d api worker video-worker`.

---

### Video (Reels) y clips

| Formato | Camino | Notas |
|---------|--------|-------|
| `reel` | VideoScript → escenas → TTS → Shotstack | Async-only; cola `video_render`; `video_gen_mode=full\|scenes\|still` |
| `user_clip_reel` | Drive → ffmpeg → Whisper → ClipEditor → Shotstack | Requiere `drive_folder_id` + ffmpeg |

Modos (`GET /api/video/options`): `full` = clip AI completo con Venice; `scenes` = Venice anima cada toma y Shotstack las une; `still` = stills + Ken Burns sin video AI. Modelos vía `venice_video_models.py` (Seedance 2.5/2.0, Kling O3, MiniMax H3).

---

### API principal

- `GET /health`, `/health/background`, `/social/publish-status`
- `GET /image/providers`, `/image/archetypes`, `/image/formats`, `/video/options`
- `GET /thoughts/{trace_id}`, `POST /thoughts/{trace_id}/reply` — hilo de pensamiento y checkpoints
- `POST /briefs/upload-asset`, `POST/GET/DELETE /briefs/brand-manual`, `POST /briefs/upload-brand-manual`
- `POST /advisor/chat`
- `POST/GET /briefs`
- `POST /runs/sync`, `/runs/async` — `content_format`, `image_provider`, `social_account_id`, `link_url`, `cta_on_image`, …; reel/clips solo async
- `POST /runs/{id}/approve`, `/reject`, `/revise`
- `GET /runs`, `/runs/{id}`
- `POST/GET /campaigns`, `POST /campaigns/{id}/fire`
- OAuth: `/auth/login|callback/{meta,linkedin,google,x}`, `/auth/status`, `/auth/accounts`, `DELETE /auth/accounts/{id}`

Estáticos: `static/uploads/`, `static/uploads/brand/`, `static/images/`, `static/videos/`, `static/fonts/`.

---

### Frontend (dashboard)

- Brief: descripción producto/evento, público, objetivo, tono
- Red social + Formato: selects acoplados servidos por `/image/formats` (+ Drive folder en `user_clip_reel`)
- Video: modo de generación y modelo Venice (solo en `reel`)
- Generador: fal / Venice / SD (según API)
- Manual de marca: upload PDF, swatches, logos, quitar
- Foto usuario + alterar con IA
- Cuenta destino, enlace, CTA en imagen opcional
- Modo interactivo + hilo de pensamiento (`AgentThoughtThread`)
- Sync/Async, historial HITL, Integraciones, burbuja Asesor

---

### Tests (271 collected — 2026-08-11)

Suite anterior + `test_format_normalization.py` (formatos por red + universal), `test_thought_stream.py`, `test_text_contrast_and_video_models.py`, `test_linkedin_native.py`, `test_brand_*`, `test_venice.py`, `test_premium_fonts.py`, `test_caption.py`, …

> 3 fallos conocidos y preexistentes en `test_venice.py` y `test_video_timeline_clips.py` (estructura del edit Shotstack); no bloquean el pipeline.

```bash
python -m pytest tests/ -q
```

---

### Cómo levantar el stack

Ver **`infra/arranque-stack.md`** (T1–T7 + T4b video).

Extra marca/OCR (una vez):

```bash
pip install pypdf pymupdf paddlepaddle paddleocr   # si OCR_PROVIDER=paddle
```

---

### Variables de entorno mínimas (ampliadas)

**Local (dev):**

```env
IMAGE_PROVIDER=fal
FAL_API_KEY=...
LLM_PROVIDER=ollama          # o anthropic / openai+OpenRouter
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5433/marketing_mvp
PUBLIC_IMAGE_BASE_URL=https://TU-NGROK   # Meta + assets locales
```

**Producción (VPS — `.env.production`):**

```env
DOMAIN=marketing.powerupsecosistem.online
LLM_PROVIDER=openai
OPENAI_API_KEY=sk-or-v1-...
OPENAI_API_BASE=https://openrouter.ai/api/v1
OPENAI_MODEL=google/gemini-2.0-flash-001
X_API_KEY=...
X_API_SECRET=...
X_REDIRECT_URI=https://marketing.powerupsecosistem.online/api/auth/callback/x
PUBLIC_IMAGE_BASE_URL=https://marketing.powerupsecosistem.online
```

Tras cambiar secretos en VPS: `docker compose -f infra/docker-compose.prod.yml --env-file .env.production up -d api worker video-worker`

**No commitear `.env` ni `.env.production`.**

---

### Roadmap vs realidad

| Paso | Estado |
|------|--------|
| 1–5 Happy path, Postgres, APIs, HITL, LangGraph Copy/QA | ✅ |
| 6 Go / K8s | 🟡 Sidecars listos; clúster real pendiente |
| 7–9 Diseño editorial, foto usuario, LinkedIn nativo | ✅ |
| 10 Canva/Figma MCP | ⬜ |
| 11–12 Reels + clips usuario | ✅ |
| 13–14 Revise HITL + multi-cuenta | ✅ |
| 15 Manual de marca (OCR + scan + campaña) | ✅ |
| 16 Venice.ai (imagen + escenas i2v) | ✅ |
| 17 Asesor creativo (chat) | ✅ |
| 18 Hilo de pensamiento + modo interactivo | ✅ |
| 19 Formatos por red + universal 1:1 | ✅ |
| 20 X publicación nativa | ✅ OAuth + publish; prod configurado |
| 21 TikTok publicación nativa | ⏳ App Review; código OAuth/publish fase 2 |
| 22 Deploy VPS + InsightFlow | ✅ Live |
| 23 OpenRouter LLM en prod | 🔄 Key + validar que no caiga a stub |
| 24 Biblioteca multi-imagen | ⬜ Modelo `UserAsset` iniciado; API/UI pendiente |
| 25 Copy sin eco del brief | ⬜ Endurecer strategist/copywriter/QA + overlay |

---

### Pendientes conocidos

1. Canva OAuth / Canva-Figma MCP
2. Video v2: música de fondo, captions por palabra
3. TikTok: publicación nativa tras App Review
4. **X:** post de prueba en prod (Integraciones o tokens en env)
5. **Meta:** verificar Conectar Meta tras Basic Settings en dominio prod
6. **Biblioteca de fotos:** subir varias imágenes reutilizables por tenant (WIP `UserAsset`)
7. **Copy/overlay:** evitar que texto del brief (tema, público, objetivo, tono) aparezca literal en imagen/caption
8. CI/CD contra GKE real + Secret Manager
9. Fallback LLM stub → propagar error a la UI
10. Revise v2: historial de versiones y notas en copy + overlay Pillow
11. Formato `universal` en video: hoy solo cubre imagen
12. LinkedIn: solo perfil personal; solo imagen; token ~60 d sin refresh auto
13. OAuth `_pending_states` en memoria (rompe con réplicas K8s)
14. OpenRouter en prod: confirmar agentes usan LLM real, no stub

---

### Resumen

MVP **end-to-end**: brief con **manual de marca**, copy con QA e inbound, piezas por red (IG, FB, LinkedIn, TikTok, X + **universal 1:1**), generadores **fal / Venice / SD**, Reels y clips Drive, asesor, hilo de pensamiento, HITL con revise, publicación **Meta/LinkedIn/X** (multi-cuenta donde aplica).

**Producción:** `marketing.powerupsecosistem.online` en VPS Hostinger junto a InsightFlow.  
**Docs:** este archivo + `README.md` + `CLAUDE.md` + `infra/arranque-stack.md` (local) + `infra/deploy/vps-hostinger.md` + `infra/deploy/proceso-integracion-redes.md`.
